{
  "id": "google-workspace",
  "name": "Google Workspace",
  "description": "Gmail and Calendar tools via Google Workspace service account",
  "skills": [
    "./skills/gmail",
    "./skills/google-calendar"
  ],
  "configSchema": {
    "type": "object",
    "properties": {
      "serviceAccountKey": {
        "type": "string",
        "description": "Path to service account JSON key file, or the JSON string itself"
      },
      "delegateEmail": {
        "type": "string",
        "description": "Google Workspace email to impersonate via domain-wide delegation"
      }
    },
    "required": ["serviceAccountKey", "delegateEmail"],
    "additionalProperties": false
  }
}
